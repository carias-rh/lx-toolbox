# Copy to ~/.config/systemd/user/lx-toolbox.env (install.sh does this).
LX_TOOLBOX_DIR=/home/carias/PycharmProjects/lx-toolbox
LX_TOOLBOX_MAIN_REF=main

# Optional: override if systemd user manager does not inherit your desktop session.
# DISPLAY=:0
# WAYLAND_DISPLAY=wayland-0
